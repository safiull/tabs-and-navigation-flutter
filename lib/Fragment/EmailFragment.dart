import 'package:flutter/material.dart';

class EmailFragment extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is email tab."),
      ),
    );
  }
}