import 'package:flutter/material.dart';

class AlarmFragment extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is alarm tab."),
      ),
    );
  }
}